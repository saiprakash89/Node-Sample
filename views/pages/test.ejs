<!DOCTYPE html>
<html>
	<head>
		<link rel='stylesheet' href='/salesforce-ux/design-system/assets/styles/salesforce-lightning-design-system.css' />
	</head>

	<body>
		<a href="/">Home</a>
  		<form method="post" action="/test">
  			<input type="text" name="orderId" style="display:none;" value=<%= id %>>
  			<h3>The order is "<%= selectList %>"</h3>
  			<br/>
			<select name="selectList" class="slds-dropdown">
  				<option value="Not Started" <% if(selectList == 'Not Started') { %> Selected <% } %> >Not Started</option>
  				<option value="In Process" <% if(selectList == 'In Process') { %> Selected <% } %> >In Process</option>
  				<option value="Shipped" <% if(selectList == 'Shipped') { %> Selected <% } %> >Shipped</option>
 				<option value="Delivered" <% if(selectList == 'Delivered') { %> Selected <% } %> >Delivered</option>
 				<option value="Returned" <% if(selectList == 'Returned') { %> Selected <% } %> >Returned</option>
			</select>
			<br/>
  			<button name="submit" class="slds-button slds-button--neutral">Submit</button>
  		</form>
	</body>
</html>